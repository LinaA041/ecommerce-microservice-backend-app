apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: microservice-reader
rules:
- apiGroups: [""]
  resources:
  - configmaps
  - secrets
  - services
  - endpoints
  verbs:
  - get
  - list
  - watch
