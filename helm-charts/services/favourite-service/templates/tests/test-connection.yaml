apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "favourite-service.fullname" . }}-test-connection"
  labels:
    {{- include "favourite-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "favourite-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
