apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proxy-client.fullname" . }}-test-connection"
  labels:
    {{- include "proxy-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proxy-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
