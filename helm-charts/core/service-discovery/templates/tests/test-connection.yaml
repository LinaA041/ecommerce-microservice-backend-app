apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-discovery.fullname" . }}-test-connection"
  labels:
    {{- include "service-discovery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-discovery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
